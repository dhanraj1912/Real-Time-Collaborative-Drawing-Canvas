services:
  - type: web
    name: realtime-collab-canvas
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        fromPort: 3000
